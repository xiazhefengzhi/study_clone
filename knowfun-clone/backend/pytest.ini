# pytest configuration
# This project does NOT use pytest for testing
# All test scripts should be run directly with Python

[pytest]
# Disable pytest collection
python_files =
python_classes =
python_functions =

# If pytest is accidentally triggered, show this message
addopts = --tb=no -p no:warnings

# Empty test paths to prevent pytest from running
testpaths =
